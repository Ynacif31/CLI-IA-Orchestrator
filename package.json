{
  "name": "orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-agent CLI orchestrator (RFC 0001)",
  "bin": {
    "orchestrator": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
